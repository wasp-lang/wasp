const SectionLabel = ({ text }: { text: string }) => (
  <div className="mb-6 inline-block bg-wasp-yellow px-4 py-1.5 font-mono font-bold text-xs text-wasp-black tracking-widest uppercase">
    // {text}
  </div>
);

export default SectionLabel;
