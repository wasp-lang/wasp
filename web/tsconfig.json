{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    // We must set the `baseUrl`, because otherwise Docusaurus's
    // tsconfig `baseUrl` overrides ours, leading to wrong paths.
    "baseUrl": ".",
    "ignoreDeprecations": "6.0"
  },
  "exclude": ["build"]
}
