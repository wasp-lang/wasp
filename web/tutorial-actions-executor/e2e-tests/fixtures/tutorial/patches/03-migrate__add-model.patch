diff --git a/schema.prisma b/schema.prisma
index 190e2a8..5dc8402 100644
--- a/schema.prisma
+++ b/schema.prisma
@@ -8,3 +8,10 @@ datasource db {
 generator client {
   provider = "prisma-client-js"
 }
+
+model Post {
+  id        Int      @id @default(autoincrement())
+  title     String
+  content   String?
+  createdAt DateTime @default(now())
+}
