app waspApp {
  wasp: {
    version: "^0.21.0"
  },
  title: "wasp-app",
  head: [
    "<link rel='icon' href='/favicon.ico' />",
  ],
  db: {
    seeds: [
      import { populateTasks } from "@src/db/populateTasks",
      import { assertTasksEmpty } from "@src/db/assertTasksEmpty",
      import { assertTasksNotEmpty } from "@src/db/assertTasksNotEmpty"
    ]
  },
}
route RootRoute { path: "/", to: MainPage }
page MainPage {
  component: import { MainPage } from "@src/MainPage"
}