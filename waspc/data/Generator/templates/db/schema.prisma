{{={= =}=}}

datasource db {
  provider = "{= datasourceProvider =}"
  url      = {=& datasourceUrl =}
}

generator client {
  provider = "prisma-client-js"
  output = env("{= prismaClientOutputDirEnvVar =}")
}

{=# modelSchemas =}
{=& . =}
{=/ modelSchemas =}
