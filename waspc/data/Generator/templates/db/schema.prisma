{{={= =}=}}

datasource db {
  provider = "{= datasourceProvider =}"
  url      = {=& datasourceUrl =}
}

generator client {
  provider = "prisma-client-js"
  output = "{= clientOutputDir =}"
}

{=# modelSchemas =}
{=& . =}
{=/ modelSchemas =}
