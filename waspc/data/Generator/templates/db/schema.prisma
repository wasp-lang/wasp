{{={= =}=}}

datasource db {
  provider = "{= datasourceProvider =}"
  url      = {=& datasourceUrl =}
}

generator client {
  provider = "prisma-client-js"
  output = env("PRISMA_CLIENT_OUTPUT_DIR")
}

{=# modelSchemas =}
{=& . =}
{=/ modelSchemas =}
