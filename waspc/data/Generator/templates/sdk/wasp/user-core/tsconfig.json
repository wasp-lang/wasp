{
  // TODO: Eventually switch to user's config.
  "extends": "../tsconfig.sdk.json",
  "compilerOptions": {
    "outDir": "../dist/user-core",
    "tsBuildInfoFile": "../dist/user-core/.tsbuildinfo",
  },
  "include": ["."],
  // TODO: Interpolate through Haskell?
  "references": [
    { "path": "../core" }
  ],
}
