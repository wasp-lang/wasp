{
  // TODO: Eventually split 'user-core' and 'src'
  // into two different tsconfig projects.
  "extends": "../tsconfig.sdk.json",
  "compilerOptions": {
    "rootDir": "..",
    "outDir": "../dist/"
  },
  "include": [".", "../src"],
  "references": [
    { "path": "../core" },
  ]
}
