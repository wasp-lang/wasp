{
  // TODO: Eventually split 'user-core' and 'src'
  // into two different tsconfig projects.
  "extends": "../tsconfig.sdk.json",
  "compilerOptions": {
    "rootDir": "..",
    "outDir": "../dist/"
  },
  "include": [".", "../src", "../core/auth/forms/internal/email/VerifyEmailForm.tsx"],
  "references": [
    { "path": "../core" },
  ]
}
