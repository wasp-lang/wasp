{{={= =}=}}
{
  "name": "{= packageName =}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  {=& depsChunk =},
  {=& devDepsChunk =},
  "scripts": {
    "start": "vite",
    "build": "tsc --build && vite build && vite build --ssr src/entry-server.tsx --outDir build-ssr",
    "preview": "node server-ssr.mjs"
  },
  "engineStrict": true,
  "engines": {
      "node": "{=& nodeVersionRange =}"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
