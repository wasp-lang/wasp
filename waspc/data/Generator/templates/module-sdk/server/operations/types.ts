{{={= =}=}}
import type { {= opKind =}, Authenticated{= opKind =} } from 'wasp/server/module';

{=# operations =}
export type {= typeName =} = {= baseType =}<{= entityType =}, any, any>;

{=/ operations =}
