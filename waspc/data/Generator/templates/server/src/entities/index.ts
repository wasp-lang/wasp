{{={= =}=}}
import {
  {=# entities =}
  {= name =},
  {=/ entities =}
} from "@prisma/client"
  
export {
  {=# entities =}
  {= name =},
  {=/ entities =}
} from "@prisma/client"

export type WaspEntity = 
  {=# entities =}
  | {= name =}
  {=/ entities =}
