{{={= =}=}}
{
  "env": {
    "NODE_ENV": "development"
  },
  "watch": [
    "src/",
    "{= relativeUserSrcDirPath =}",
    ".env"
  ],
  "comment-filip": "We now have to watch {= relativeUserSrcDirPath =} because we're importing client files directly",
  "ext": "ts,mts,js,mjs,json"
}
