{{={= =}=}}
{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "nodemon ./src/server.js",
    "debug": "DEBUG=server:* npm start",
    "standard": "standard"
  },
  "engines": {
    "node": ">={= nodeVersion =}"
  },
  {=& depsChunk =},
  "devDependencies": {
    "nodemon": "^2.0.4",
    "standard": "^14.3.4",
    "@prisma/cli": "2.x"
  }
}
