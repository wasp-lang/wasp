{{={= =}=}}
{
  "name": "{= packageName =}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    {=# areDbSeedsDefined =}
    "db-seed": "{=& dbSeedScript =}",
    {=/ areDbSeedsDefined =}
    "db-migrate-prod": "prisma migrate deploy --schema=../db/schema.prisma",
    "start-production": "{=& startProductionScript =}"
  },
  "engineStrict": true,
  "engines": {
    "node": "{=& nodeVersionRange =}"
  },
  "prisma": {=& prisma =},
  {=& depsChunk =},
  {=& devDepsChunk =}
}
