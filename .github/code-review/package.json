{
  "name": "code-review",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "npm run typecheck && node --test \"tests/*.test.ts\"",
    "generate-inputs": "node commands/generate-inputs.ts",
    "load-review-context": "node commands/load-review-context.ts",
    "publish-review": "node commands/publish-review.ts",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@octokit/plugin-paginate-graphql": "^6.0.0",
    "@octokit/rest": "^22.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
