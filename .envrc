# Enters the Nix dev shell automatically when you cd into the repo.
# Requires direnv (https://direnv.net) — nix-direnv is recommended for
# caching (https://github.com/nix-community/nix-direnv).
use flake
